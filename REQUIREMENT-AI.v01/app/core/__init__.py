"""Core package initialization for Requirement-AI.

This file intentionally avoids importing submodules to prevent circular imports
when modules like `engine` import others within the same package.
"""

